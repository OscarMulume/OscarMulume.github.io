<!DOCTYPE html>
<html lang="fr" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Oscar Mulume — IT Consultant & Head of Engineering. Sécurisation, modernisation et automatisation de vos infrastructures." />
  <meta name="theme-color" content="#0a0a0f" />
  <title>Oscar Mulume | IT Consultant & Head of Engineering</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --bg-primary: #0a0a0f;
      --bg-secondary: #111118;
      --bg-card: rgba(255,255,255,0.03);
      --border: rgba(255,255,255,0.08);
      --border-hover: rgba(255,255,255,0.15);
      --text-primary: #f0f0f5;
      --text-secondary: #8888aa;
      --accent: #3b82f6;
      --accent-glow: rgba(59,130,246,0.15);
      --accent-bright: #60a5fa;
    }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--bg-primary);
      color: var(--text-primary);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
      overflow-x: hidden;
    }

    /* ─── Animated background ─── */
    .bg-grid {
      position: fixed; inset: 0; z-index: 0;
      background-image:
        linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
      background-size: 64px 64px;
      pointer-events: none;
    }
    .bg-glow {
      position: fixed; border-radius: 50%; filter: blur(120px); pointer-events: none; z-index: 0;
    }
    .bg-glow-1 { width: 600px; height: 600px; top: -200px; right: -100px; background: rgba(59,130,246,0.06); }
    .bg-glow-2 { width: 400px; height: 400px; bottom: 10%; left: -100px; background: rgba(99,102,241,0.04); }

    /* ─── Navigation ─── */
    .nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      padding: 1rem 0;
      transition: all 0.3s ease;
      border-bottom: 1px solid transparent;
    }
    .nav.scrolled {
      background: rgba(10,10,15,0.85);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
    }
    .nav-inner {
      max-width: 1200px; margin: 0 auto; padding: 0 2rem;
      display: flex; justify-content: space-between; align-items: center;
    }
    .nav-logo {
      font-size: 1.25rem; font-weight: 800; color: var(--text-primary); text-decoration: none; letter-spacing: -0.02em;
    }
    .nav-logo span { color: var(--accent); }
    .nav-links { display: flex; gap: 2.5rem; align-items: center; }
    .nav-links a {
      color: var(--text-secondary); text-decoration: none; font-size: 0.875rem; font-weight: 500;
      transition: color 0.2s; letter-spacing: 0.01em;
    }
    .nav-links a:hover { color: var(--text-primary); }
    .nav-cta {
      padding: 0.5rem 1.25rem; border: 1px solid var(--accent); border-radius: 8px;
      background: var(--accent-glow); color: var(--accent-bright); text-decoration: none;
      font-size: 0.875rem; font-weight: 600; transition: all 0.2s;
    }
    .nav-cta:hover { background: rgba(59,130,246,0.25); border-color: var(--accent-bright); }

    /* ─── Hero ─── */
    .hero {
      min-height: 100vh; display: flex; align-items: center; position: relative;
      padding: 8rem 2rem 4rem; max-width: 1200px; margin: 0 auto;
    }
    .hero-content { max-width: 720px; position: relative; z-index: 1; }
    .hero-badge {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.375rem 1rem; border-radius: 100px;
      background: rgba(59,130,246,0.08); border: 1px solid rgba(59,130,246,0.2);
      font-size: 0.75rem; font-weight: 600; color: var(--accent-bright);
      letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 2rem;
    }
    .hero-badge-dot { width: 6px; height: 6px; border-radius: 50%; background: #22c55e; animation: pulse-dot 2s ease-in-out infinite; }
    @keyframes pulse-dot { 0%,100% { opacity:1; } 50% { opacity:0.4; } }

    .hero h1 {
      font-size: clamp(2.5rem, 7vw, 5rem); font-weight: 900; line-height: 1.05;
      letter-spacing: -0.03em; margin-bottom: 1.5rem;
    }
    .hero h1 .line { display: block; }
    .hero h1 .accent {
      background: linear-gradient(135deg, var(--accent-bright) 0%, #818cf8 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
    }
    .hero-subtitle {
      font-size: clamp(1.125rem, 2vw, 1.375rem); color: var(--text-secondary);
      font-weight: 400; max-width: 560px; margin-bottom: 2.5rem; line-height: 1.7;
    }
    .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }
    .btn-primary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.875rem 1.75rem; border-radius: 10px; font-size: 0.9375rem; font-weight: 600;
      text-decoration: none; transition: all 0.25s ease; cursor: pointer;
      background: linear-gradient(135deg, #3b82f6, #2563eb);
      color: white; border: 1px solid rgba(255,255,255,0.1);
      box-shadow: 0 4px 24px rgba(59,130,246,0.25), inset 0 1px 0 rgba(255,255,255,0.1);
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 40px rgba(59,130,246,0.35), inset 0 1px 0 rgba(255,255,255,0.15);
    }
    .btn-secondary {
      display: inline-flex; align-items: center; gap: 0.5rem;
      padding: 0.875rem 1.75rem; border-radius: 10px; font-size: 0.9375rem; font-weight: 600;
      text-decoration: none; transition: all 0.25s ease;
      background: var(--bg-card); color: var(--text-primary);
      border: 1px solid var(--border);
    }
    .btn-secondary:hover { border-color: var(--border-hover); transform: translateY(-2px); }

    /* ─── Stats ─── */
    .stats {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem;
      margin-top: 4rem; padding-top: 2.5rem; border-top: 1px solid var(--border);
    }
    .stat-num { font-size: 1.75rem; font-weight: 800; color: var(--text-primary); letter-spacing: -0.02em; }
    .stat-label { font-size: 0.8125rem; color: var(--text-secondary); margin-top: 0.25rem; }

    /* ─── Section shared ─── */
    section { position: relative; z-index: 1; }
    .section-wrap { max-width: 1200px; margin: 0 auto; padding: 6rem 2rem; }
    .section-header { text-align: center; margin-bottom: 4rem; }
    .section-label {
      display: inline-block; font-size: 0.75rem; font-weight: 700; letter-spacing: 0.12em;
      text-transform: uppercase; color: var(--accent-bright); margin-bottom: 1rem;
    }
    .section-title { font-size: clamp(2rem, 4vw, 3rem); font-weight: 800; letter-spacing: -0.03em; }
    .section-desc { font-size: 1.0625rem; color: var(--text-secondary); max-width: 560px; margin: 1rem auto 0; }

    /* ─── Services (cards with glow border) ─── */
    .cards-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 1.5rem; }
    .card {
      position: relative; padding: 2rem; border-radius: 16px;
      background: var(--bg-card); border: 1px solid var(--border);
      transition: all 0.3s ease; overflow: hidden;
    }
    .card::before {
      content: ''; position: absolute; inset: 0; border-radius: 16px;
      background: radial-gradient(400px circle at var(--mouse-x, 50%) var(--mouse-y, 50%), rgba(59,130,246,0.06), transparent 60%);
      opacity: 0; transition: opacity 0.3s; pointer-events: none;
    }
    .card:hover::before { opacity: 1; }
    .card:hover { border-color: rgba(59,130,246,0.25); transform: translateY(-4px); }
    .card-icon {
      width: 44px; height: 44px; border-radius: 12px; margin-bottom: 1.5rem;
      background: rgba(59,130,246,0.1); border: 1px solid rgba(59,130,246,0.15);
      display: flex; align-items: center; justify-content: center;
    }
    .card-icon svg { width: 22px; height: 22px; color: var(--accent-bright); }
    .card h3 { font-size: 1.125rem; font-weight: 700; margin-bottom: 0.75rem; letter-spacing: -0.01em; }
    .card p { font-size: 0.9375rem; color: var(--text-secondary); line-height: 1.7; }

    /* ─── Timeline ─── */
    .timeline { max-width: 720px; margin: 0 auto; }
    .tl-item { position: relative; padding-left: 2.5rem; padding-bottom: 3rem; border-left: 2px solid var(--border); }
    .tl-item:last-child { border-left-color: transparent; padding-bottom: 0; }
    .tl-dot {
      position: absolute; left: -7px; top: 0; width: 12px; height: 12px; border-radius: 50%;
      background: var(--accent); border: 3px solid var(--bg-primary);
      box-shadow: 0 0 0 3px rgba(59,130,246,0.15);
    }
    .tl-dot.past { background: var(--text-secondary); box-shadow: none; }
    .tl-header { display: flex; flex-wrap: wrap; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem; }
    .tl-title { font-size: 1.125rem; font-weight: 700; }
    .tl-badge {
      padding: 0.25rem 0.75rem; border-radius: 100px; font-size: 0.75rem; font-weight: 600;
    }
    .tl-badge.now { background: rgba(34,197,94,0.1); color: #22c55e; border: 1px solid rgba(34,197,94,0.2); }
    .tl-badge.past { background: rgba(255,255,255,0.05); color: var(--text-secondary); }
    .tl-badge.done { background: rgba(34,197,94,0.1); color: #22c55e; border: 1px solid rgba(34,197,94,0.2); }
    .tl-company { font-size: 0.875rem; color: var(--accent-bright); font-weight: 500; margin-bottom: 0.5rem; }
    .tl-desc { font-size: 0.9375rem; color: var(--text-secondary); line-height: 1.7; }

    /* ─── Certifications ─── */
    .cert-row { display: flex; flex-wrap: wrap; justify-content: center; gap: 1rem; }
    .cert-badge {
      display: flex; align-items: center; gap: 0.625rem;
      padding: 0.75rem 1.5rem; border-radius: 12px;
      background: var(--bg-card); border: 1px solid var(--border);
      font-size: 0.875rem; font-weight: 500; color: var(--text-primary);
    }

    /* ─── CV Section ─── */
    .cv-card {
      max-width: 640px; margin: 0 auto; padding: 2.5rem; border-radius: 20px;
      background: var(--bg-card); border: 1px solid var(--border);
    }
    .cv-profile { display: flex; align-items: center; gap: 1.5rem; }
    .cv-avatar {
      width: 80px; height: 80px; border-radius: 20px; flex-shrink: 0;
      background: linear-gradient(135deg, #3b82f6, #6366f1);
      display: flex; align-items: center; justify-content: center;
      font-size: 1.75rem; font-weight: 800; color: white;
    }
    .cv-name { font-size: 1.25rem; font-weight: 700; }
    .cv-role { font-size: 0.875rem; color: var(--accent-bright); font-weight: 600; }
    .cv-loc { font-size: 0.8125rem; color: var(--text-secondary); margin-top: 0.125rem; }
    .cv-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-top: 2rem; }
    .cv-grid-label { font-size: 0.75rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.06em; }
    .cv-grid-val { font-size: 0.9375rem; color: var(--text-primary); font-weight: 500; margin-top: 0.25rem; }
    .cv-actions { display: flex; gap: 1rem; margin-top: 2rem; flex-wrap: wrap; }

    /* ─── Contact ─── */
    .contact-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1.25rem; max-width: 800px; margin: 0 auto; }
    .contact-card {
      padding: 1.5rem; border-radius: 16px; text-decoration: none;
      background: var(--bg-card); border: 1px solid var(--border);
      transition: all 0.3s ease; display: block;
    }
    .contact-card:hover { border-color: rgba(59,130,246,0.25); transform: translateY(-3px); }
    .contact-icon {
      width: 40px; height: 40px; border-radius: 10px; margin-bottom: 1rem;
      background: rgba(59,130,246,0.1); display: flex; align-items: center; justify-content: center;
    }
    .contact-icon svg { width: 18px; height: 18px; color: var(--accent-bright); }
    .contact-label { font-size: 0.9375rem; font-weight: 600; color: var(--text-primary); }
    .contact-val { font-size: 0.8125rem; color: var(--text-secondary); margin-top: 0.25rem; }

    /* ─── Footer ─── */
    .footer {
      border-top: 1px solid var(--border); padding: 2rem; text-align: center;
      font-size: 0.8125rem; color: var(--text-secondary); position: relative; z-index: 1;
    }

    /* ─── Mobile ─── */
    .mobile-btn { display: none; background: none; border: none; color: var(--text-primary); cursor: pointer; padding: 0.5rem; }
    .mobile-menu { display: none; position: absolute; top: 100%; left: 0; right: 0; background: rgba(10,10,15,0.95); backdrop-filter: blur(20px); border-bottom: 1px solid var(--border); padding: 1.5rem; flex-direction: column; gap: 1.25rem; }
    .mobile-menu.open { display: flex; }
    .mobile-menu a { color: var(--text-secondary); text-decoration: none; font-size: 1rem; font-weight: 500; }

    @media (max-width: 768px) {
      .nav-links, .nav-cta-desktop { display: none; }
      .mobile-btn { display: block; }
      .hero { padding: 6rem 1.5rem 3rem; }
      .stats { grid-template-columns: 1fr; gap: 1.5rem; }
      .cards-grid { grid-template-columns: 1fr; }
      .cv-profile { flex-direction: column; text-align: center; }
      .cv-grid { grid-template-columns: 1fr; }
      .cv-actions { justify-content: center; }
    }
  </style>
</head>
<body>

<div class="bg-grid"></div>
<div class="bg-glow bg-glow-1"></div>
<div class="bg-glow bg-glow-2"></div>

<!-- Nav -->
<nav class="nav" id="nav">
  <div class="nav-inner">
    <a href="/" class="nav-logo">Oscar<span>Mulume</span></a>
    <div class="nav-links">
      <a href="#services">Services</a>
      <a href="#parcours">Parcours</a>
      <a href="#cv">CV</a>
      <a href="#contact">Contact</a>
    </div>
    <a href="#contact" class="nav-cta nav-cta-desktop">Me contacter</a>
    <button class="mobile-btn" id="mobileBtn" aria-label="Menu">
      <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 6h16M4 12h16M4 18h16"/></svg>
    </button>
  </div>
  <div class="mobile-menu" id="mobileMenu">
    <a href="#services">Services</a>
    <a href="#parcours">Parcours</a>
    <a href="#cv">CV</a>
    <a href="#contact">Contact</a>
    <a href="#contact" class="nav-cta">Me contacter</a>
  </div>
</nav>

<!-- Hero -->
<section class="hero">
  <div class="hero-content">
    <div class="hero-badge">
      <span class="hero-badge-dot"></span>
      Disponible pour de nouvelles missions
    </div>
    <h1>
      <span class="line">Oscar</span>
      <span class="line accent">Mulume</span>
    </h1>
    <p class="hero-subtitle">
      IT Consultant &amp; Head of Engineering — Sécurisation, modernisation et automatisation de vos infrastructures. De l'audit réseau à l'intégration d'agents IA.
    </p>
    <div class="hero-actions">
      <a href="#services" class="btn-primary">
        Explorer mes services
        <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17l9-9-9-9M16 8H2"/></svg>
      </a>
      <a href="#cv" class="btn-secondary">Consulter mon CV</a>
    </div>
    <div class="stats">
      <div><div class="stat-num">5+</div><div class="stat-label">Ans d'expérience</div></div>
      <div><div class="stat-num">10+</div><div class="stat-label">Projets livrés</div></div>
      <div><div class="stat-num">100%</div><div class="stat-label">Satisfaction client</div></div>
    </div>
  </div>
</section>

<!-- Services -->
<section id="services">
  <div class="section-wrap">
    <div class="section-header">
      <div class="section-label">Expertise</div>
      <h2 class="section-title">Services de Consultance</h2>
      <p class="section-desc">Trois pôles d'expertise pour répondre à tous vos défis technologiques</p>
    </div>
    <div class="cards-grid">
      <div class="card">
        <div class="card-icon">
          <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        </div>
        <h3>Infrastructures &amp; Réseaux</h3>
        <p>Audit, conception et modernisation de vos réseaux d'entreprise. Déploiement d'équipements de pointe (Cisco, architecture UniFi) et intégration de solutions haut débit.</p>
      </div>
      <div class="card">
        <div class="card-icon">
          <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
        </div>
        <h3>Cybersécurité Offensive &amp; Défensive</h3>
        <p>Sécurisation des terminaux, analyse de vulnérabilités et tests d'intrusion rigoureux pour garantir l'intégrité de vos données critiques.</p>
      </div>
      <div class="card">
        <div class="card-icon">
          <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>
        </div>
        <h3>Développement &amp; IA</h3>
        <p>Développement de solutions logicielles sur mesure (Web &amp; Mobile) et recherche sur l'intégration d'agents IA autonomes pour optimiser vos processus.</p>
      </div>
    </div>
  </div>
</section>

<!-- Parcours -->
<section id="parcours">
  <div class="section-wrap">
    <div class="section-header">
      <div class="section-label">Carrière</div>
      <h2 class="section-title">Parcours Professionnel</h2>
      <p class="section-desc">Une expertise construite sur le terrain, de l'ingénierie réseau à la direction technique</p>
    </div>
    <div class="timeline">
      <div class="tl-item">
        <div class="tl-dot"></div>
        <div class="tl-header">
          <span class="tl-title">IT Consultant &amp; Head of Engineering</span>
          <span class="tl-badge now">● Depuis Mars 2026</span>
        </div>
        <div class="tl-company">Malaïka Group</div>
        <div class="tl-desc">Pilotage des stratégies technologiques, supervision des infrastructures réseau et de l'ingénierie interne. Management des équipes techniques et veille technologique.</div>
      </div>
      <div class="tl-item">
        <div class="tl-dot past"></div>
        <div class="tl-header">
          <span class="tl-title">BSc Networks &amp; Telecoms</span>
          <span class="tl-badge past">2025</span>
        </div>
        <div class="tl-company">ISIPA, Kinshasa</div>
        <div class="tl-desc">Maîtrise des architectures réseaux, de la maintenance système et des protocoles de télécommunication. Spécialisation en sécurité des infrastructures.</div>
      </div>
      <div class="tl-item">
        <div class="tl-dot past"></div>
        <div class="tl-header">
          <span class="tl-title">Advanced Diploma in Computer Science</span>
          <span class="tl-badge done">✓ Diplôme Obtenu</span>
        </div>
        <div class="tl-company">ISIG, Goma</div>
        <div class="tl-desc">Solides fondations en sciences de l'informatique et développement logiciel. Bases en programmation, bases de données et administration système.</div>
      </div>
    </div>

    <div style="margin-top:4rem; padding-top:2.5rem; border-top:1px solid rgba(255,255,255,0.08);">
      <h3 style="text-align:center; font-size:1.125rem; font-weight:700; margin-bottom:2rem;">Certifications</h3>
      <div class="cert-row">
        <div class="cert-badge">
          <svg width="18" height="18" fill="#eab308" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>
          Cisco CCNA
        </div>
      </div>
    </div>
  </div>
</section>

<!-- CV -->
<section id="cv">
  <div class="section-wrap">
    <div class="section-header">
      <div class="section-label">Profil</div>
      <h2 class="section-title">Mon CV</h2>
      <p class="section-desc">Consultez mon parcours détaillé ou téléchargez-le au format PDF</p>
    </div>
    <div class="cv-card">
      <div class="cv-profile">
        <div class="cv-avatar">OM</div>
        <div>
          <div class="cv-name">Oscar Mulume</div>
          <div class="cv-role">IT Consultant &amp; Head of Engineering</div>
          <div class="cv-loc">Kinshasa, RDC</div>
        </div>
      </div>
      <div class="cv-grid">
        <div><div class="cv-grid-label">Formation</div><div class="cv-grid-val">BSc Networks &amp; Telecoms</div></div>
        <div><div class="cv-grid-label">Certification</div><div class="cv-grid-val">Cisco CCNA</div></div>
        <div><div class="cv-grid-label">Expertise</div><div class="cv-grid-val">Réseaux, Sécurité, IA</div></div>
        <div><div class="cv-grid-label">Expérience</div><div class="cv-grid-val">5+ ans</div></div>
      </div>
      <div class="cv-actions">
        <a href="/cv-oscar-mulume.pdf" target="_blank" class="btn-primary">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
          Télécharger CV (PDF)
        </a>
        <a href="#contact" class="btn-secondary">Me contacter</a>
      </div>
    </div>
  </div>
</section>

<!-- Contact -->
<section id="contact">
  <div class="section-wrap">
    <div class="section-header">
      <div class="section-label">Connexion</div>
      <h2 class="section-title">Contact</h2>
      <p class="section-desc">Un projet, une question ? N'hésitez pas à me contacter</p>
    </div>
    <div class="contact-grid">
      <a href="mailto:oscarmulume1612@gmail.com" class="contact-card">
        <div class="contact-icon"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg></div>
        <div class="contact-label">Email</div>
        <div class="contact-val">oscarmulume1612@gmail.com</div>
      </a>
      <a href="https://www.linkedin.com/in/oscar-mulume-95666b225/" target="_blank" rel="noopener noreferrer" class="contact-card">
        <div class="contact-icon"><svg fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg></div>
        <div class="contact-label">LinkedIn</div>
        <div class="contact-val">Oscar Mulume</div>
      </a>
      <a href="https://github.com/OscarMulume" target="_blank" rel="noopener noreferrer" class="contact-card">
        <div class="contact-icon"><svg fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg></div>
        <div class="contact-label">GitHub</div>
        <div class="contact-val">@OscarMulume</div>
      </a>
      <a href="tel:+243975585150" class="contact-card">
        <div class="contact-icon"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg></div>
        <div class="contact-label">Téléphone</div>
        <div class="contact-val">+243 975 585 150</div>
      </a>
    </div>
  </div>
</section>

<!-- Footer -->
<footer class="footer">
  <span id="year"></span> Oscar Mulume. Tous droits réservés.
</footer>

<script>
  // Nav scroll
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => nav.classList.toggle('scrolled', window.scrollY > 50));

  // Mobile menu
  const mBtn = document.getElementById('mobileBtn');
  const mMenu = document.getElementById('mobileMenu');
  mBtn?.addEventListener('click', () => mMenu.classList.toggle('open'));
  mMenu?.querySelectorAll('a').forEach(a => a.addEventListener('click', () => mMenu.classList.remove('open')));

  // Mouse glow on cards
  document.querySelectorAll('.card').forEach(card => {
    card.addEventListener('mousemove', e => {
      const r = card.getBoundingClientRect();
      card.style.setProperty('--mouse-x', (e.clientX - r.left) + 'px');
      card.style.setProperty('--mouse-y', (e.clientY - r.top) + 'px');
    });
  });

  // Year
  document.getElementById('year').textContent = new Date().getFullYear();
</script>
</body>
</html>
